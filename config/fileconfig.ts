export const FILE_CONFIG = {
  maxFileSize: 2 * 1024 * 1024,
  maxFileSizeStr: '2Mo',
  allowedImageExtensions: /jpeg|jpg|png|svg/
}

export const FILE_CONFIG_WITH_PDF = {
  maxFileSize: 2 * 1024 * 1024,
  maxFileSizeStr: '2Mo',
  allowedImageExtensions: /jpeg|jpg|png|pdf/
}


export const FILE_CONFIG_WITH_PDF_AND_DOC = {
  maxFileSize: 4 * 1024 * 1024,
  maxFileSizeStr: '4Mo',
  allowedImageExtensions: /jpeg|jpg|png|pdf|doc|docx|xls|xlsx|ppt|pptx/
}
